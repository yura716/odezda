# 🚂 Развертывание на Railway

## 📖 Обзор

Railway - современная PaaS платформа для быстрого деплоя приложений.

**Преимущества:**
- ✅ **Очень простой** деплой (5-10 минут)
- ✅ **Автоматический** деплой из GitHub
- ✅ **Бесплатно** для начала ($5 в месяц кредитов)
- ✅ **HTTPS** из коробки
- ✅ **Логи и мониторинг** встроены

---

## 💰 Стоимость

### Бесплатный план:
- **$5 кредитов/месяц** бесплатно
- Хватит на ~500 часов работы
- Отлично для тестирования

### Hobby план ($5/мес):
- **$5 кредитов + $5 за дополнительные ресурсы**
- Примерно **$10-20/мес** при умеренном трафике

### Калькулятор расходов:
- Backend + Frontend работают 24/7: **~$7-10/мес**
- Плюс API расходы (OpenAI, NanoBanana)

---

## 🚀 Быстрый старт (10 минут)

### Шаг 1: Подготовьте GitHub репозиторий

#### 1.1 Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите **New repository**
3. Название: `odezda`
4. Public или Private
5. Нажмите **Create repository**

#### 1.2 Загрузите код

**На вашем Mac:**

```bash
cd /Users/urij/Documents/odezda

# Инициализируйте Git (если еще не сделали)
git init

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Initial commit for Railway deployment"

# Подключите к GitHub
git remote add origin https://github.com/ваш-username/odezda.git
git branch -M main
git push -u origin main
```

---

### Шаг 2: Зарегистрируйтесь на Railway

1. Зайдите на https://railway.app
2. Нажмите **Login**
3. Войдите через **GitHub**
4. Разрешите доступ Railway к GitHub

---

### Шаг 3: Создайте проект

1. В Railway dashboard нажмите **New Project**
2. Выберите **Deploy from GitHub repo**
3. Выберите репозиторий `odezda`
4. Railway автоматически определит Python и Node.js

---

### Шаг 4: Настройте Backend

#### 4.1 Railway автоматически создаст сервис

Railway создаст сервис для backend. Теперь настроим его:

1. Нажмите на сервис **backend**
2. Перейдите в **Settings**

#### 4.2 Настройте Root Directory

В **Settings** → **Source**:
- **Root Directory:** `backend`
- **Build Command:** (оставьте пустым, Railway сам определит)
- **Start Command:** `uvicorn main:app --host 0.0.0.0 --port $PORT`

#### 4.3 Добавьте переменные окружения

В **Variables** добавьте:

```
OPENAI_API_KEY=ваш_ключ_openai
NANOBANANA_API_KEY=ваш_ключ_nanobanana
HOST=0.0.0.0
PORT=$PORT
ALLOWED_ORIGINS=https://ваш-frontend.railway.app
```

**Важно:** `ALLOWED_ORIGINS` обновите после создания frontend (шаг 5)

#### 4.4 Получите URL backend

1. В **Settings** → **Networking**
2. Нажмите **Generate Domain**
3. Скопируйте URL, например: `https://odezda-backend-production.up.railway.app`

---

### Шаг 5: Настройте Frontend

#### 5.1 Создайте второй сервис

1. В проекте нажмите **New Service**
2. Выберите **Deploy from GitHub repo**
3. Выберите тот же репозиторий `odezda`

#### 5.2 Настройте Root Directory

В **Settings** → **Source**:
- **Root Directory:** `frontend`
- **Build Command:** `npm install && npm run build`
- **Start Command:** `npx serve -s build -l $PORT`

#### 5.3 Добавьте переменные окружения

В **Variables** добавьте:

```
REACT_APP_API_URL=https://odezda-backend-production.up.railway.app
NODE_ENV=production
```

Замените URL на ваш backend URL из шага 4.4!

#### 5.4 Получите URL frontend

1. В **Settings** → **Networking**
2. Нажмите **Generate Domain**
3. Скопируйте URL, например: `https://odezda-frontend-production.up.railway.app`

---

### Шаг 6: Обновите CORS

Вернитесь к **backend сервису**:

1. **Variables**
2. Обновите `ALLOWED_ORIGINS`:

```
ALLOWED_ORIGINS=https://odezda-frontend-production.up.railway.app
```

3. Railway автоматически передеплоит backend

---

### Шаг 7: Проверка!

Откройте в браузере:
```
https://odezda-frontend-production.up.railway.app
```

Должен работать сайт! 🎉

---

## 🎯 Альтернативная схема: Один сервис с Nginx

Более эффективно - использовать один сервис с Nginx:

### 1. Создайте railway.json

В корне проекта:

```bash
cat > /Users/urij/Documents/odezda/railway.json << 'EOF'
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh start_railway.sh",
    "healthcheckPath": "/api",
    "healthcheckTimeout": 100
  }
}
EOF
```

### 2. Создайте скрипт запуска

```bash
cat > /Users/urij/Documents/odezda/start_railway.sh << 'EOF'
#!/bin/bash

# Установите зависимости backend
cd backend
pip install -r requirements.txt
cd ..

# Соберите frontend
cd frontend
npm install
npm run build
cd ..

# Установите serve для frontend
npm install -g serve

# Запустите backend в фоне
cd backend
python main.py &
BACKEND_PID=$!
cd ..

# Запустите frontend
cd frontend
serve -s build -l $PORT
EOF

chmod +x /Users/urij/Documents/odezda/start_railway.sh
```

### 3. Загрузите на GitHub

```bash
git add railway.json start_railway.sh
git commit -m "Add Railway configuration"
git push
```

Railway автоматически передеплоит!

---

## 🔧 Создание файлов конфигурации для Railway

Railway использует **Nixpacks** для автоопределения и сборки. Но можно настроить вручную:

### Backend: Procfile

```bash
cat > /Users/urij/Documents/odezda/backend/Procfile << 'EOF'
web: uvicorn main:app --host 0.0.0.0 --port $PORT
EOF
```

### Frontend: package.json

Убедитесь что в `frontend/package.json` есть:

```json
{
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "serve": "serve -s build"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
```

Добавьте `serve` если его нет:

```bash
cd /Users/urij/Documents/odezda/frontend
npm install --save serve
```

---

## 🌐 Настройка собственного домена (опционально)

### 1. Купите домен

На Namecheap, GoDaddy и т.д.

### 2. В Railway:

1. Откройте сервис (frontend)
2. **Settings** → **Networking** → **Custom Domain**
3. Введите ваш домен: `odezda.com`
4. Railway покажет CNAME запись

### 3. У регистратора домена:

Добавьте CNAME запись:
```
Type: CNAME
Name: @ (или www)
Value: odezda-frontend-production.up.railway.app
```

### 4. SSL сертификат

Railway автоматически создаст SSL сертификат (бесплатно)!

---

## 📊 Мониторинг и логи

### Логи в реальном времени:

1. Откройте сервис в Railway
2. Перейдите на вкладку **Deployments**
3. Нажмите на последний деплой
4. Смотрите логи в реальном времени

### Метрики:

1. **Metrics** → видите использование CPU, RAM, Network
2. **Usage** → расходы в $

### Алерты:

Railway может присылать уведомления:
1. **Settings** → **Notifications**
2. Настройте Webhook или Email

---

## 🔄 Автоматический деплой

Railway автоматически деплоит при каждом push в GitHub!

```bash
# Сделайте изменения
nano backend/main.py

# Закоммитьте
git add .
git commit -m "Update backend"
git push

# Railway автоматически деплоит!
```

Следите за деплоем в Railway dashboard.

---

## 💸 Оптимизация расходов

### 1. Используйте Sleep Mode

Railway может "усыплять" неактивные сервисы:

1. **Settings** → **Environment**
2. Включите **Sleep on Inactivity**
3. Сервис будет спать после 5 минут без запросов

**Но:** первый запрос после сна будет медленным (~10-20 сек)

### 2. Используйте один сервис

Вместо двух (backend + frontend) используйте один с Nginx.

### 3. Оптимизируйте образ

Удалите ненужные зависимости:

```bash
# В requirements.txt оставьте только нужное
# В package.json удалите dev-dependencies
```

---

## 🔒 Безопасность

### 1. Переменные окружения

**Никогда** не коммитьте `.env` файлы!

Проверьте `.gitignore`:
```
.env
backend/.env
frontend/.env
```

### 2. Rate Limiting

Добавьте ограничение запросов (см. PUBLIC_ACCESS.md)

### 3. Мониторьте расходы

Railway → **Usage** → следите за расходами!

Настройте лимит:
1. **Settings** → **Billing**
2. **Spending Limit** → установите лимит

---

## 📈 Масштабирование

### Vertical scaling (больше ресурсов):

Railway автоматически выделяет ресурсы, но можно настроить:

1. **Settings** → **Resources**
2. Увеличьте CPU/RAM

### Horizontal scaling (больше инстансов):

В платных планах можно запустить несколько реплик:

1. **Settings** → **Replicas**
2. Увеличьте количество

---

## 🆘 Решение проблем

### Backend не запускается

**Проверьте логи:**
1. **Deployments** → последний деплой
2. Смотрите ошибки

**Частые проблемы:**
- ❌ Неверный `PORT` → используйте `$PORT`
- ❌ Нет зависимостей → проверьте `requirements.txt`
- ❌ Python версия → Railway использует 3.9+

### Frontend не собирается

**Проверьте:**
- `npm run build` работает локально?
- Все зависимости в `package.json`?
- `REACT_APP_API_URL` правильный?

### CORS ошибки

**Проверьте переменные backend:**
```
ALLOWED_ORIGINS=https://ваш-frontend.railway.app
```

Должен быть **точный** URL frontend!

### Сайт медленный

**Sleep Mode включен?**
- Отключите в Settings
- Или используйте платный план

---

## 💡 Советы

### 1. Используйте Railway CLI

Установите:
```bash
npm install -g @railway/cli
railway login
```

Теперь можно деплоить из терминала:
```bash
railway up
```

### 2. Настройте Preview Environments

Railway создает превью для pull requests:

1. **Settings** → **Environments**
2. Включите **PR Deploys**

### 3. Используйте Railway Volumes

Для постоянного хранения файлов:

1. **Settings** → **Volumes**
2. Добавьте volume для `/app/uploads`

---

## 🔄 Обновление проекта

```bash
# На вашем Mac
cd /Users/urij/Documents/odezda

# Сделайте изменения
nano backend/main.py

# Закоммитьте
git add .
git commit -m "Update feature"
git push

# Railway автоматически деплоит!
# Следите за прогрессом в dashboard
```

---

## 📊 Сравнение Railway с другими

| Критерий | Railway | Beget | VPS | Домашний |
|----------|---------|-------|-----|----------|
| **Стоимость** | $10-20/мес | ₽200-400/мес | €4.5/мес | ~₽500/мес |
| **Сложность** | ⭐ Легко | ⭐⭐ Средне | ⭐⭐⭐ Сложно | ⭐⭐ Средне |
| **Скорость** | ⚡ Быстро | ⚡ Средне | ⚡⚡ Очень быстро | ⚡ Зависит |
| **Для РФ** | ⚠️ Может быть медленно | ✅ Отлично | ⚠️ Зависит | ✅ Отлично |
| **Автодеплой** | ✅ Да | ❌ Нет | ⚠️ Настроить | ❌ Нет |
| **HTTPS** | ✅ Автоматически | ✅ Да | ⚠️ Настроить | ⚠️ Настроить |

---

## 🎯 Рекомендации

**Railway идеален если:**
- ✅ Нужен быстрый запуск (5-10 минут)
- ✅ Не хотите настраивать сервер
- ✅ Нужен автодеплой из GitHub
- ✅ Бюджет $10-20/мес

**НЕ рекомендую Railway если:**
- ❌ Аудитория только в России (может быть медленно)
- ❌ Очень ограниченный бюджет (Beget дешевле)
- ❌ Нужен полный контроль (VPS лучше)

---

## 📋 Чек-лист

- [ ] Создали GitHub репозиторий
- [ ] Загрузили код на GitHub
- [ ] Зарегистрировались на Railway
- [ ] Создали проект из GitHub
- [ ] Настроили backend сервис
- [ ] Добавили переменные окружения backend
- [ ] Получили URL backend
- [ ] Настроили frontend сервис
- [ ] Добавили переменные окружения frontend
- [ ] Обновили CORS в backend
- [ ] Проверили что сайт работает
- [ ] (Опционально) Настроили домен
- [ ] Настроили лимит расходов

---

## 📚 Полезные ссылки

- Railway: https://railway.app
- Документация: https://docs.railway.app
- Discord: https://discord.gg/railway
- Примеры: https://railway.app/templates

---

## 🎉 Готово!

Ваш сайт работает на Railway!

**Адреса:**
- Frontend: `https://odezda-frontend-production.up.railway.app`
- Backend: `https://odezda-backend-production.up.railway.app`

**Следующие шаги:**
1. Добавьте rate limiting (защита API)
2. Настройте свой домен
3. Следите за расходами
4. Наслаждайтесь автодеплоем! 🚀

**Удачи!** 🎉✨

