# 🎯 Первый запуск Odezda AI

## Приветствуем! 👋

Это ваш первый запуск Odezda AI - умного AI-стилиста. Следуйте этим шагам для успешного старта.

---

## ✅ Чек-лист перед запуском

Убедитесь, что у вас есть:

- [ ] **Python 3.8+** установлен
  ```bash
  python3 --version
  ```

- [ ] **Node.js 16+** установлен
  ```bash
  node --version
  ```

- [ ] **OpenAI API ключ** 
  - Получить: https://platform.openai.com/api-keys
  - На балансе есть минимум $5

- [ ] **Интернет соединение**
  - Для установки зависимостей
  - Для работы с OpenAI API

---

## 🚀 Пошаговая инструкция

### Шаг 1: Проверьте систему (30 секунд)

```bash
cd /Users/urij/Documents/odezda
./check_system.sh
```

Этот скрипт проверит все требования и укажет на проблемы.

---

### Шаг 2: Настройте OpenAI ключ (2 минуты)

#### 2.1. Получите API ключ

1. Откройте https://platform.openai.com/api-keys
2. Войдите или создайте аккаунт
3. Нажмите **"Create new secret key"**
4. Дайте имя ключу (например: "Odezda")
5. Скопируйте ключ (начинается с `sk-`)

⚠️ **Важно**: Сохраните ключ - он показывается только один раз!

#### 2.2. Добавьте ключ в проект

```bash
cd backend
```

Если файл `.env` не существует, создайте его из шаблона:
```bash
cp env_example.txt .env
```

Откройте файл `.env` в любом редакторе:
```bash
nano .env
# или
code .env
# или
open -a TextEdit .env
```

Замените `your_openai_api_key_here` на ваш реальный ключ:
```bash
OPENAI_API_KEY=sk-proj-ваш-реальный-ключ-здесь
```

Сохраните и закройте файл.

---

### Шаг 3: Первый запуск (5-10 минут)

#### Вариант А: Автоматический (рекомендуется)

```bash
cd /Users/urij/Documents/odezda
./start.sh
```

Скрипт автоматически:
- ✅ Создаст виртуальное окружение для Python
- ✅ Установит Python зависимости (~2 минуты)
- ✅ Установит Node.js зависимости (~3 минуты)
- ✅ Запустит backend на порту 8000
- ✅ Запустит frontend на порту 3000
- ✅ Откроет браузер

**Ожидайте**: При первом запуске установка занимает 5-10 минут.

---

#### Вариант Б: Ручной запуск

**Терминал 1 - Backend:**

```bash
cd /Users/urij/Documents/odezda
./start_backend.sh
```

Вы должны увидеть:
```
✅ Запускаю сервер на http://localhost:8000
INFO:     Started server process
INFO:     Uvicorn running on http://0.0.0.0:8000
```

**Терминал 2 - Frontend:**

Откройте новый терминал и выполните:

```bash
cd /Users/urij/Documents/odezda
./start_frontend.sh
```

Вы должны увидеть:
```
✅ Запускаю приложение на http://localhost:3000
Compiled successfully!

You can now view odezda-frontend in the browser.
```

---

### Шаг 4: Проверьте работу (1 минута)

#### 4.1. Проверьте Backend

Откройте в браузере: http://localhost:8000

Вы должны увидеть:
```json
{"message":"Odezda AI API работает!"}
```

Проверьте health: http://localhost:8000/api/health

```json
{
  "status": "healthy",
  "openai_configured": true
}
```

✅ Если `openai_configured: true` - всё настроено правильно!

#### 4.2. Проверьте Frontend

Откройте в браузере: http://localhost:3000

Вы должны увидеть красивую страницу с:
- 👗 Заголовком "Odezda"
- 📸 Формой для загрузки фото
- 🎨 Выбором стилей одежды

---

### Шаг 5: Первый анализ (30 секунд)

1. **Подготовьте фото**
   - Ваше фото в полный рост или по пояс
   - Хорошее освещение
   - Формат: JPG или PNG
   - Размер: до 10MB

2. **Загрузите фото**
   - Нажмите на область загрузки
   - Выберите фото
   - Увидите предпросмотр

3. **Выберите стиль**
   - Выберите один из предложенных стилей
   - Или введите свой в текстовое поле

4. **Нажмите "Подобрать одежду"**
   - Ждите 10-15 секунд
   - Увидите индикатор загрузки

5. **Изучите результаты**
   - Анализ вашей внешности
   - 5-7 рекомендаций одежды
   - Ссылки на магазины
   - Советы по стилю

---

## 🎉 Успех!

Поздравляем! Вы успешно запустили Odezda AI!

### Что дальше?

#### Для пользователей:
- 📸 Попробуйте разные фото
- 🎨 Экспериментируйте со стилями
- 🛍️ Переходите по ссылкам в магазины
- 💡 Читайте [EXAMPLES.md](EXAMPLES.md) для идей

#### Для разработчиков:
- 🔧 Изучите [API.md](API.md)
- 📖 Читайте [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)
- 🐛 Настройте отладку
- 🤝 Вносите вклад: [CONTRIBUTING.md](CONTRIBUTING.md)

#### Для тех, кто хочет деплоить:
- 🐳 Изучите [DOCKER.md](DOCKER.md)
- 🌐 Выберите хостинг (Railway, Vercel)
- 🔒 Настройте безопасность

---

## ❗ Возможные проблемы при первом запуске

### Backend не запускается

**Симптом**: Ошибка при запуске `start_backend.sh`

**Решения**:

1. Проверьте Python версию:
   ```bash
   python3 --version  # Должно быть 3.8+
   ```

2. Проверьте .env файл:
   ```bash
   cat backend/.env | grep OPENAI_API_KEY
   ```
   Должна быть строка с вашим ключом.

3. Проверьте порт 8000:
   ```bash
   lsof -i :8000
   ```
   Если занят, остановите процесс или измените порт в `backend/.env`

4. Установите зависимости вручную:
   ```bash
   cd backend
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   python main.py
   ```

---

### Frontend не запускается

**Симптом**: Ошибка при запуске `start_frontend.sh`

**Решения**:

1. Проверьте Node.js версию:
   ```bash
   node --version  # Должно быть 16+
   ```

2. Удалите node_modules и переустановите:
   ```bash
   cd frontend
   rm -rf node_modules package-lock.json
   npm install
   npm start
   ```

3. Проверьте порт 3000:
   ```bash
   lsof -i :3000
   ```
   React автоматически предложит другой порт если 3000 занят.

---

### Ошибка OpenAI API

**Симптом**: "Ошибка соединения с сервером" при анализе

**Решения**:

1. Проверьте API ключ:
   ```bash
   cat backend/.env
   ```
   Убедитесь, что ключ правильный и начинается с `sk-`

2. Проверьте баланс OpenAI:
   - Откройте https://platform.openai.com/usage
   - Убедитесь, что есть доступные средства

3. Проверьте лимиты:
   - Убедитесь, что у вас есть доступ к GPT-4o

4. Тестовый запрос:
   ```bash
   cd backend
   source venv/bin/activate
   python -c "from openai import OpenAI; import os; from dotenv import load_dotenv; load_dotenv(); print('Testing...'); client = OpenAI(api_key=os.getenv('OPENAI_API_KEY')); r = client.chat.completions.create(model='gpt-4o', messages=[{'role':'user','content':'Hi'}]); print('Success!')"
   ```

---

### Frontend не видит Backend

**Симптом**: Ошибка CORS или "Network Error"

**Решения**:

1. Убедитесь, что backend запущен:
   ```bash
   curl http://localhost:8000/api/health
   ```

2. Проверьте URL в frontend:
   ```bash
   cat frontend/.env
   ```
   Должно быть: `REACT_APP_API_URL=http://localhost:8000`

3. Проверьте CORS в backend:
   - Откройте `backend/main.py`
   - Найдите `ALLOWED_ORIGINS`
   - Убедитесь, что там есть `http://localhost:3000`

4. Откройте консоль браузера (F12):
   - Ищите красные ошибки
   - Проверьте вкладку Network

---

## 📊 Использование ресурсов

При первом запуске:

| Действие | Время | Место на диске |
|----------|-------|----------------|
| Установка Python зависимостей | 2-3 мин | ~100 MB |
| Установка Node.js зависимостей | 3-5 мин | ~300 MB |
| Запуск приложения | 10 сек | - |
| Один анализ фото | 10-15 сек | $0.01-0.03 |

**Всего на диске**: ~450-500 MB

---

## 🔄 Последующие запуски

После первой установки запуск будет быстрым:

```bash
# Терминал 1
./start_backend.sh

# Терминал 2  
./start_frontend.sh
```

Или в одной команде:
```bash
./start.sh
```

Время запуска: **~30 секунд** 🚀

---

## 📞 Нужна помощь?

1. **FAQ**: [FAQ.md](FAQ.md) - ответы на частые вопросы
2. **Документация**: [README.md](README.md) - полная документация
3. **Примеры**: [EXAMPLES.md](EXAMPLES.md) - идеи использования
4. **Логи**: Проверьте вывод в терминале и консоль браузера (F12)

---

## ✨ Советы для лучшего опыта

1. **Качество фото**: Используйте фото с хорошим освещением
2. **Будьте конкретны**: "Минимализм для офиса" лучше чем просто "минимализм"
3. **Экспериментируйте**: Попробуйте разные стили с одним фото
4. **Сохраняйте**: Делайте скриншоты понравившихся результатов
5. **Читайте объяснения**: AI объясняет, почему рекомендует именно эту одежду

---

**Удачи! 🎉 Пусть AI поможет вам найти идеальный стиль!**

_P.S. Если что-то не работает - не паникуйте! Проверьте раздел "Возможные проблемы" выше._

