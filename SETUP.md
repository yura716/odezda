# 🚀 Инструкция по установке и запуску

## Требования

- Python 3.8+
- Node.js 16+
- OpenAI API ключ

## Шаг 1: Настройка Backend

### 1. Перейдите в директорию backend

```bash
cd backend
```

### 2. Создайте виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate  # На macOS/Linux
# или
venv\Scripts\activate  # На Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Откройте `.env` и добавьте ваш OpenAI API ключ:

```
OPENAI_API_KEY=sk-your-api-key-here
```

**Где получить API ключ:**
1. Зарегистрируйтесь на https://platform.openai.com/
2. Перейдите в раздел API keys
3. Создайте новый ключ

### 5. Запустите сервер

```bash
python main.py
```

Сервер запустится на `http://localhost:8000`

Проверьте работоспособность: http://localhost:8000/api/health

## Шаг 2: Настройка Frontend

### 1. Откройте новый терминал и перейдите в директорию frontend

```bash
cd frontend
```

### 2. Установите зависимости

```bash
npm install
```

### 3. Настройте переменные окружения (опционально)

Если backend работает не на localhost:8000, создайте файл `.env`:

```bash
cp .env.example .env
```

И измените URL в `.env`:

```
REACT_APP_API_URL=http://your-backend-url:8000
```

### 4. Запустите приложение

```bash
npm start
```

Приложение откроется в браузере на `http://localhost:3000`

## 🎉 Готово!

Теперь вы можете:
1. Открыть http://localhost:3000 в браузере
2. Загрузить свое фото
3. Выбрать желаемый стиль одежды
4. Получить персональные рекомендации от AI

## Возможные проблемы

### Backend не запускается

- Проверьте, что установлены все зависимости: `pip list`
- Убедитесь, что Python версии 3.8+: `python --version`
- Проверьте, что порт 8000 свободен

### Frontend не подключается к Backend

- Убедитесь, что backend запущен и доступен
- Проверьте настройки CORS в `backend/main.py`
- Проверьте URL в `frontend/.env`

### Ошибка OpenAI API

- Проверьте, что API ключ корректный в `backend/.env`
- Убедитесь, что у вас есть доступ к GPT-4 Vision API
- Проверьте баланс на вашем аккаунте OpenAI

## Разработка

### Backend

Для автоматической перезагрузки при изменениях:

```bash
uvicorn main:app --reload
```

### Frontend

React автоматически перезагружается при изменениях файлов.

## Деплой

### Backend

Рекомендуется использовать:
- Railway
- Render
- DigitalOcean App Platform
- AWS/GCP/Azure

### Frontend

Рекомендуется использовать:
- Vercel (самый простой вариант)
- Netlify
- GitHub Pages

## Дополнительные возможности

### Интеграция с реальными магазинами

В файле `backend/main.py` функция `search_products()` возвращает демо-ссылки.
Для интеграции с реальными магазинами:

1. Получите API ключи от:
   - Lamoda API
   - Wildberries API
   - Ozon Seller API

2. Реализуйте поиск товаров через их API
3. Замените функцию `search_products()` на реальную реализацию

### Улучшение AI анализа

Вы можете изменить промпт в функции `analyze_image_and_style()` для:
- Более детального анализа
- Учета сезона
- Учета бюджета
- Анализа цветотипа

## Поддержка

При возникновении проблем:
1. Проверьте логи backend
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все зависимости установлены


