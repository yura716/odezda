# 🤝 Руководство по внесению вклада

Спасибо за интерес к проекту Odezda AI! Мы рады любому вкладу.

## Как внести вклад

### 🐛 Сообщить об ошибке

1. Проверьте, что ошибка еще не зарегистрирована в Issues
2. Создайте новый Issue с подробным описанием:
   - Операционная система
   - Версии Python/Node.js
   - Шаги для воспроизведения
   - Ожидаемое и фактическое поведение
   - Скриншоты (если применимо)

### 💡 Предложить новую функцию

1. Создайте Issue с тегом "enhancement"
2. Опишите:
   - Какую проблему решает функция
   - Как она должна работать
   - Примеры использования

### 🔧 Внести изменения в код

1. **Fork репозиторий**
   ```bash
   git clone https://github.com/your-username/odezda.git
   cd odezda
   ```

2. **Создайте ветку для изменений**
   ```bash
   git checkout -b feature/amazing-feature
   ```

3. **Внесите изменения**
   - Следуйте стилю кода проекта
   - Добавьте комментарии где необходимо
   - Обновите документацию

4. **Протестируйте изменения**
   ```bash
   # Backend
   cd backend
   python -m pytest
   
   # Frontend
   cd frontend
   npm test
   ```

5. **Commit изменений**
   ```bash
   git add .
   git commit -m "feat: добавлена новая функция"
   ```

6. **Push в ваш fork**
   ```bash
   git push origin feature/amazing-feature
   ```

7. **Создайте Pull Request**

## 📝 Стиль кода

### Python (Backend)

- Следуйте **PEP 8**
- Используйте **type hints**
- Пишите docstrings для функций
- Максимальная длина строки: 100 символов

```python
def analyze_image(image_data: bytes, style: str) -> dict:
    """
    Анализирует изображение и подбирает одежду.
    
    Args:
        image_data: Байты изображения
        style: Желаемый стиль одежды
        
    Returns:
        Словарь с рекомендациями
    """
    # код здесь
    pass
```

### JavaScript (Frontend)

- Используйте **ES6+** синтаксис
- Предпочитайте **функциональные компоненты** (React Hooks)
- Используйте **const/let** вместо var
- Добавляйте **PropTypes** или TypeScript types

```javascript
const MyComponent = ({ data, onAction }) => {
  const [state, setState] = useState(null);
  
  // код здесь
  
  return <div>...</div>;
};
```

### CSS

- Используйте **осмысленные имена классов**
- Следуйте **BEM методологии** где возможно
- Избегайте **!important**
- Используйте **CSS переменные** для цветов

```css
.button-primary {
  background: var(--primary-color);
  padding: 1rem 2rem;
}
```

## 🧪 Тестирование

### Backend

```bash
cd backend
pip install pytest pytest-asyncio
python -m pytest tests/
```

Пример теста:
```python
def test_analyze_image():
    response = client.post("/api/analyze", ...)
    assert response.status_code == 200
    assert "recommendations" in response.json()
```

### Frontend

```bash
cd frontend
npm test
```

## 📚 Документация

При добавлении новой функции обновите:
- `README.md` - основное описание
- `API.md` - если добавили новый эндпоинт
- `FAQ.md` - если есть важные вопросы
- Комментарии в коде

## 🌟 Приоритетные задачи

Мы особенно приветствуем помощь в:

### Backend
- [ ] Добавление тестов
- [ ] Кеширование результатов
- [ ] Rate limiting
- [ ] Реальная интеграция с API магазинов
- [ ] Оптимизация обработки изображений

### Frontend
- [ ] Мобильная версия
- [ ] Темная тема
- [ ] Анимации и transitions
- [ ] Accessibility (A11Y)
- [ ] Интернационализация (i18n)

### Документация
- [ ] Видео-туториалы
- [ ] Больше примеров использования
- [ ] Переводы на другие языки
- [ ] API документация с Swagger

### DevOps
- [ ] Docker контейнеры
- [ ] CI/CD pipeline
- [ ] Мониторинг и логирование
- [ ] Автоматическое тестирование

## 🎨 Дизайн

При работе над UI/UX:
- Следуйте существующему стилю
- Используйте цветовую палитру проекта
- Тестируйте на разных размерах экрана
- Убедитесь в доступности (контрастность, alt тексты)

## 🔒 Безопасность

При нахождении уязвимости:
1. **НЕ создавайте публичный Issue**
2. Свяжитесь с мейнтейнерами напрямую
3. Дайте время на исправление перед публикацией

## ⚖️ Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш код будет распространяться под той же лицензией, что и проект.

## 💬 Общение

- **Issues**: для багов и предложений
- **Pull Requests**: для изменений кода
- **Discussions**: для общих вопросов

## 🎉 Признание

Все контрибьюторы будут добавлены в раздел благодарностей в README!

---

**Спасибо, что делаете Odezda AI лучше! 🙏**


