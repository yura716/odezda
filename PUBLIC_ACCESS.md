# 🌐 Публичный доступ к локальному серверу

## 📖 Обзор

Этот гайд покажет как дать доступ к вашему сайту другим людям без развертывания на сервере.

**Принцип работы:**
```
Другой человек
    ↓
Интернет
    ↓
ngrok туннель
    ↓
Ваш компьютер (localhost)
```

---

## 🚀 Автоматический способ (Рекомендуется)

### 1. Установите ngrok

```bash
brew install ngrok
```

Или скачайте с https://ngrok.com/download

### 2. Зарегистрируйтесь (бесплатно)

1. Зайдите на https://ngrok.com
2. Создайте аккаунт
3. Скопируйте authtoken с dashboard
4. Выполните:

```bash
ngrok config add-authtoken ваш_токен
```

### 3. Запустите всё одной командой!

```bash
cd /Users/urij/Documents/odezda
./start_public.sh
```

Скрипт автоматически:
- ✅ Запустит backend
- ✅ Запустит frontend  
- ✅ Создаст ngrok туннель
- ✅ Покажет публичный URL

### 4. Обновите CORS (один раз)

После получения ngrok URL, добавьте его в `backend/.env`:

```bash
ALLOWED_ORIGINS=http://localhost:3000,https://abc123.ngrok.io
```

Затем перезапустите скрипт.

### 5. Поделитесь ссылкой!

Отправьте людям URL:
```
https://abc123.ngrok.io
```

---

## 🛠️ Ручной способ

Если хотите больше контроля:

### Терминал 1 - Backend
```bash
cd /Users/urij/Documents/odezda/backend
source venv/bin/activate
python main.py
```

### Терминал 2 - Frontend
```bash
cd /Users/urij/Documents/odezda/frontend
npm start
```

### Терминал 3 - ngrok
```bash
ngrok http 3000
```

Скопируйте URL из вывода ngrok и поделитесь им!

---

## 🌟 Альтернатива: Cloudflare Tunnel (Бесплатно)

Более стабильная альтернатива ngrok с постоянным URL:

### 1. Установите cloudflared

```bash
brew install cloudflare/cloudflare/cloudflared
```

### 2. Авторизуйтесь

```bash
cloudflared tunnel login
```

### 3. Создайте туннель

```bash
cloudflared tunnel create odezda
```

### 4. Настройте конфиг

Создайте `~/.cloudflared/config.yml`:

```yaml
tunnel: odezda
credentials-file: /Users/urij/.cloudflared/<tunnel-id>.json

ingress:
  - hostname: odezda.вашдомен.com
    service: http://localhost:3000
  - service: http_status:404
```

### 5. Запустите туннель

```bash
cloudflared tunnel run odezda
```

**Преимущества:**
- ✅ URL не меняется
- ✅ Можно использовать свой домен
- ✅ Полностью бесплатно
- ✅ Более стабильно

---

## ⚙️ Настройка CORS

Чтобы frontend мог общаться с backend через ngrok, обновите `backend/.env`:

### Для ngrok:
```bash
ALLOWED_ORIGINS=http://localhost:3000,https://abc123.ngrok.io
```

### Для Cloudflare:
```bash
ALLOWED_ORIGINS=http://localhost:3000,https://odezda.вашдомен.com
```

**После изменения перезапустите backend!**

---

## 📊 Мониторинг

### ngrok Dashboard
Откройте http://localhost:4040 чтобы видеть:
- Все запросы в реальном времени
- Статистику
- Ошибки

### Логи

```bash
# Backend логи
tail -f /tmp/odezda_backend.log

# Frontend логи
tail -f /tmp/odezda_frontend.log
```

---

## ⚠️ Важные ограничения

### 1. Компьютер должен быть включен
- Если выключите Mac → сайт станет недоступен
- Если потеряете интернет → сайт станет недоступен

### 2. Бесплатный ngrok
- ✅ 1 туннель одновременно
- ✅ 40 подключений/минуту
- ⚠️ URL меняется при каждом перезапуске
- ⚠️ Сессия ограничена 2 часами

### 3. Платный ngrok ($8/мес)
- ✅ Постоянный URL
- ✅ Больше подключений
- ✅ Свой домен
- ✅ Без ограничения времени

### 4. Производительность
- Зависит от вашего интернета
- Может быть медленнее чем на сервере
- Ваш Mac будет под нагрузкой

---

## 🔒 Безопасность

### ⚠️ Риски публичного доступа:

1. **API ключи** - люди тратят ваши деньги на OpenAI/NanoBanana
2. **Нагрузка** - много запросов могут нагрузить ваш Mac
3. **DDoS** - злоумышленники могут атаковать

### 🛡️ Защита:

#### Добавьте rate limiting

В `backend/main.py` добавьте:

```python
from fastapi import Request
from collections import defaultdict
import time

request_counts = defaultdict(list)

def check_rate_limit(ip: str, limit: int = 5, window: int = 3600):
    """Ограничение: limit запросов в window секунд"""
    now = time.time()
    request_counts[ip] = [t for t in request_counts[ip] if now - t < window]
    
    if len(request_counts[ip]) >= limit:
        return False
    
    request_counts[ip].append(now)
    return True

@app.post("/api/analyze")
async def analyze_photo(request: Request, ...):
    client_ip = request.client.host
    if not check_rate_limit(client_ip, limit=5, window=3600):
        raise HTTPException(
            status_code=429, 
            detail="Слишком много запросов. Попробуйте через час."
        )
    # ... остальной код
```

#### Используйте пароль

Добавьте простую аутентификацию в frontend:

```javascript
const password = prompt("Введите пароль:");
if (password !== "ваш_секретный_пароль") {
  alert("Неверный пароль!");
  return;
}
```

---

## 💸 Стоимость

### Бесплатный вариант:
- **ngrok:** бесплатно (с ограничениями)
- **Cloudflare:** полностью бесплатно
- **OpenAI:** ~$0.03-0.06 за запрос
- **NanoBanana:** ~$0.02-0.05 за изображение

**Следите за расходами на API!**

### Сколько это стоит при использовании?

**10 человек протестируют:**
- 10 запросов × $0.05 = **$0.50**

**100 человек:**
- 100 запросов × $0.05 = **$5**

**1000 человек:**
- 1000 запросов × $0.05 = **$50**

⚠️ **Без защиты боты могут потратить сотни долларов за ночь!**

---

## 📋 Чек-лист перед запуском

- [ ] ngrok установлен и авторизован
- [ ] backend/.env настроен с API ключами
- [ ] CORS обновлен с ngrok URL
- [ ] Rate limiting добавлен (рекомендуется)
- [ ] Понимаете что API расходы = ваши деньги
- [ ] Готовы следить за логами и расходами
- [ ] Mac будет включен все время тестирования

---

## 🎯 Рекомендации

### Для короткого теста (1-2 часа):
✅ **Используйте бесплатный ngrok**
- Запустите `./start_public.sh`
- Поделитесь ссылкой с 5-10 друзьями
- Следите за логами

### Для длительного теста (несколько дней):
✅ **Используйте Cloudflare Tunnel**
- Постоянный URL
- Более стабильно
- Можно свой домен

### Для продакшна:
✅ **Разверните на сервере** (см. DEPLOYMENT.md)
- Не зависит от вашего компьютера
- Быстрее и стабильнее
- Профессиональнее

---

## 🆘 Решение проблем

### ngrok URL не работает

**Проверьте:**
```bash
# CORS настроен?
cat backend/.env | grep ALLOWED_ORIGINS

# Backend запущен?
curl http://localhost:8000

# Frontend запущен?
curl http://localhost:3000
```

### "Слишком много запросов"

- Лимит ngrok достигнут
- Обновите до платной версии
- Или используйте Cloudflare Tunnel

### Backend падает

```bash
# Проверьте логи
tail -f /tmp/odezda_backend.log

# Возможно закончились кредиты на API
# Проверьте баланс OpenAI и NanoBanana
```

### Mac тормозит

- Много одновременных запросов
- Закройте другие приложения
- Или разверните на сервере

---

## 📚 Полезные ссылки

- ngrok: https://ngrok.com
- Cloudflare Tunnel: https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/
- Деплой на сервер: [DEPLOYMENT.md](DEPLOYMENT.md)

---

## 🎉 Готово!

Теперь вы можете дать доступ к сайту друзьям для тестирования прямо с вашего Mac!

**Удачи!** 🚀

